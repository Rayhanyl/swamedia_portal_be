# Copy to `.env` (gitignored) before running `docker compose up`.
#
#   cp .env.example .env
#
# This password is used to initialize the `postgres` container. It must be identical to
# `dbPassword` in Config.docker.toml, since that's what the app uses to connect to it.
POSTGRES_PASSWORD=REPLACE_WITH_REAL_DB_PASSWORD
